<!DOCTYPE html>
<html>
  <head>
    <title>Pedometer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    <main class="container">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>

      <div class="form-group p-2">
        <%= link_to "Log in", new_user_session_path, class: "btn btn-primary btn-block btn-lg" unless current_user %>
      </div>

      <div class="form-group p-2">
        <%= button_to "Log out", destroy_user_session_path, class: "btn btn-primary btn-block btn-lg", method: :delete if current_user %>
      </div>

      <%= yield %>
    </main>
  </body>
</html>
