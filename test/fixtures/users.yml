# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined. If you add columns to the
# model remove the "{}" from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
confirmed_user:
  email: confirmed_user@mail.com
  confirmed_at: <%= Time.zone.now %>
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

confirmed_user_with_time_zone:
  email: confirmed_user_with_time_zone@mail.com
  time_zone: 'Berlin'
  confirmed_at: <%= Time.zone.now %>
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

confirmed_user_with_activities:
  email: confirmed_user_with_activities@mail.com
  confirmed_at: <%= Time.zone.now %>
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>

another_confirmed_user_with_activities:
  email: another_confirmed_user_with_activities@mail.com
  confirmed_at: <%= Time.zone.now %>
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
